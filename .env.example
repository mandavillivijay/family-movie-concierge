# Used only by scripts/fetchTmdbData.ts, a local dev-only script that enriches
# src/data/catalog.seed.json at curation time. This key never ships in the
# deployed app and this file is gitignored once copied to .env.
TMDB_API_KEY=
